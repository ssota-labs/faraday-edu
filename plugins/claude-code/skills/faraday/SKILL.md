---
name: faraday
description: Deprecated. Use the 3d-stem skill instead — fullscreen 3D STEM interactive textbooks. This stub only redirects; do not scaffold vinext/@faraday-academy lessons.
---

# Faraday → 3d-stem (deprecated)

The **`faraday`** skill name and `npx @faraday-academy/cli` product path are
**retired**.

Use **[`3d-stem`](../3d-stem/SKILL.md)** instead:

```bash
node <repo>/skills/3d-stem/scripts/stem.mjs scaffold <name> --json
node <repo>/skills/3d-stem/scripts/stem.mjs check --dir <lesson> --json
```

Install: `npx skills add ssota-labs/faraday-academy` (loads `3d-stem`).
