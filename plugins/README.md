# Faraday Academy plugins

Marketplace wrappers around the canonical **`skills/3d-stem`** product skill.

| Host | Path |
|---|---|
| Claude Code | [`claude-code/`](claude-code/) |
| Codex | [`codex/`](codex/) |

Sync mirrors from the repo root:

```bash
pnpm sync:skills
pnpm sync:skills:check
```

Canonical SSOT: [`skills/3d-stem/`](../skills/3d-stem/). The old `faraday` skill
name is a deprecation stub only.
